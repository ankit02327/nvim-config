--SELF REFERENCE - ANKIT
--This file is for the plugins that don't require much configuration.
--Also the names point to GitHub repositories.The first word is the name of the GitHub user who created the plugin and the second word is the name of the plugin itself.
--So, if you are looking at a plugin at github, you just need to follow the above mentioned format if you want to install that plugin.

return {
  "nvim-lua/plenary.nvim", -- lua functions that many plugins use
  "christoomey/vim-tmux-navigator", -- tmux & split window navigation
}
